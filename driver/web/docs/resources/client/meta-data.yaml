post:
  tags:
    - Client
  summary: Create meta data
  description: |
    Create meta data
  security:
    - bearerAuth: []
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../schemas/apis/client/meta-data/request/Request.yaml"
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: "../../schemas/application/MetaData.yaml"
    400:
      description: Bad request
    401:
      description: Unauthorized
    500:
      description: Internal error
